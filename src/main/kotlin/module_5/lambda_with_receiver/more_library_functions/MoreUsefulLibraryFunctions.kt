package module_5.lambda_with_receiver.more_library_functions

/*
with
run
let
apply
also

                                        { .. this .. }              { .. it .. }
return result of lambda                   with / run                    let
return receiver                             apply                       also

receiver.apply {
    this.actions()
}

receiver.also {
    moreActions(it)
}
 */