package module_5.sequences.library_functions

/*
filter.size -> count
sortedBy.reversed -> sortedByDescending
map.filterNotNull -> mapNotNull

if (person.age !in map) {
  map[person.age] = mutableListOf()
}
map.getValue(person.age) += person
to getOrPut or better groupBy
 */
