package module_3.functional_programming.operations_quiz

/*
Destructuring assignment
val (youngest, oldest) = heroes.partition { it.age < 30 }

map[unknownKey] vs map.getValue(unknownKey)

map[unknownKey] returns nullable value
map.getValue(unknownKey) throws NoSuchElementException
 */